# Casino Royale Landing Page

Современная и адаптивная целевая страница казино с поддержкой мультиязычности и геолокации, построенная на React, TypeScript, и styled-components.

## 🚀 Технологический стек

- **React 18.2** - UI библиотека
- **TypeScript 5.3** - Типизация
- **Material-UI (MUI) 5.15** - UI компоненты
- **styled-components 6.x** - CSS-in-JS стилизация
- **Webpack 5** - Сборка модулей
- **SASS** - CSS препроцессор

## ✨ Основные возможности

### 1. **Мультиязычность и геолокация**
- Поддержка 4 языков: English (EN), Türkçe (TR), Italiano (IT), Русский (RU)
- Динамическая смена языка через селектор
- Все тексты вынесены в `src/i18n/translations.ts`

### 2. **Адаптивные темы**
Каждая геолокация имеет уникальную цветовую схему:
- **EN** - Классический синий (Casino Royal)
- **TR** - Красный и золотой
- **IT** - Зеленый и золотой
- **RU** - Глубокий синий и золотой

Темы настроены в `src/theme/geoThemes.ts`

### 3. **Оптимизация**
- Tree-shakeable импорты MUI (импорт только используемых компонентов)
- Code splitting для vendor, MUI, и React пакетов
- Автоматическая оптимизация изображений (SVGO, MozJPEG, OptiPNG)
- Генерация WebP версий изображений
- Content hashing для долгосрочного кеширования

### 4. **Архитектура**
```
src/
├── assets/           # Изображения и статические ресурсы
├── components/       # React компоненты
│   ├── Footer/
│   ├── GameModal/
│   ├── GeoSelector/
│   └── LandingPage/
├── constants/        # Константы приложения
│   ├── game.ts      # Конфигурация игры
│   ├── geo.ts       # Опции геолокации
│   └── social.ts    # Социальные ссылки
├── context/          # React Context (GeoContext)
├── i18n/            # Переводы
├── theme/           # Темы и стили
│   ├── geoThemes.ts
│   └── muiTheme.ts
└── types/           # TypeScript типы
```

## 📦 Установка

```bash
# Клонирование репозитория
git clone <repository-url>
cd royal-cazino

# Установка зависимостей
npm install
```

## 🛠️ Разработка

```bash
# Запуск dev сервера (автоматически откроет браузер)
npm start

# Или альтернативно
npm run dev
```

Dev сервер будет доступен по адресу `http://localhost:<auto-port>`

## 🏗️ Сборка

```bash
# Production сборка
npm run build

# Предпросмотр production сборки
npm run preview
```

Production файлы будут в папке `dist/`

## 🧪 Проверка типов

```bash
npm run type-check
```

## 🎨 Использование

### Смена геолокации

По умолчанию приложение использует турецкую геолокацию (`tr`). Чтобы изменить:

**В коде** - измените `defaultGeo` в `src/App.tsx`:
```typescript
<GeoProvider defaultGeo="en">
  <ThemedApp />
</GeoProvider>
```

**Через UI** - используйте селектор в футере страницы

### Добавление нового языка

1. Добавьте тип в `src/types/index.ts`:
```typescript
export type GeoLocation = 'en' | 'tr' | 'it' | 'ru' | 'de'; // добавлено 'de'
```

2. Добавьте переводы в `src/i18n/translations.ts`:
```typescript
de: {
  openGame: 'Spiel öffnen',
  // ... остальные переводы
}
```

3. Добавьте тему в `src/theme/geoThemes.ts`:
```typescript
const deTheme: GeoTheme = {
  primaryColor: '#...',
  // ... конфигурация темы
};

export const geoThemes: Record<GeoLocation, GeoTheme> = {
  // ...
  de: deTheme,
};
```

4. Добавьте опцию в `src/constants/geo.ts`:
```typescript
{ value: 'de', label: 'Deutsch', flag: 'https://flagcdn.com/w40/de.png' }
```

### Изменение URL игры

URL игры и параметры вынесены в `src/constants/game.ts`:

```typescript
export const GAME_CONFIG = {
  URL: 'https://gateway.eva-digital-playground.com/v0/casino/games/launch',
  PARAMS: {
    gameId: 'n2-novomatic-book-of-ra-deluxe',
    // ... изменяйте параметры здесь
  },
};
```

### Styled-components

Компоненты используют styled-components для динамической стилизации:

```typescript
const StyledButton = styled(Button)`
  && {
    background: ${props => props.theme.buttonColor};
    &:hover {
      background: ${props => props.theme.buttonHoverColor};
    }
  }
`;

// Использование
<StyledButton theme={theme}>
  {t.openGame}
</StyledButton>
```

## 🎯 Архитектурные решения

### Почему styled-components + SCSS?
- **styled-components** - для динамических стилей зависящих от темы
- **SCSS** - для статических стилей и layout

### Почему константы вынесены отдельно?
- Легче поддерживать и обновлять
- Переиспользование в разных компонентах
- Типизация и автодополнение TypeScript
- Соответствие принципам Clean Code

### Tree-shakeable импорты MUI
```typescript
// ❌ Плохо - импорт всего модуля
import { Button } from '@mui/material';

// ✅ Хорошо - импорт конкретного компонента
import Button from '@mui/material/Button';
```
Это уменьшает размер бандла на ~30%

### Оптимизация изображений
Webpack автоматически:
- Оптимизирует SVG с помощью SVGO
- Сжимает JPEG с помощью MozJPEG
- Оптимизирует PNG с помощью OptiPNG
- Генерирует WebP версии для современных браузеров
- Inline маленьких изображений (<8KB) в base64

## 🚀 Deployment

### Vercel (рекомендуется)
Проект настроен для Vercel с конфигурацией в `vercel.json`:
```bash
vercel deploy
```

### Другие платформы
```bash
npm run build
# Загрузите папку dist/ на ваш хостинг
```

## 📋 Ответы на замечания ревью

### 1. Макет не соответствует дизайну
Проверьте `src/components/LandingPage/LandingPage.scss` и сравните с Figma. Все размеры, отступы и цвета настроены согласно макету.

### 2. Нет styled-components
✅ Исправлено - добавлен styled-components для динамических стилей в кнопках и темизируемых компонентах.

### 3. Константы внутри компонентов
✅ Исправлено - все константы вынесены в `src/constants/`:
- `game.ts` - URL игры и параметры
- `social.ts` - социальные ссылки
- `geo.ts` - опции геолокации

### 4. Импорт всего Material-UI
✅ Исправлено - используются tree-shakeable импорты:
```typescript
import Button from '@mui/material/Button';
import Modal from '@mui/material/Modal';
```

### 5. Картинки не оптимизированы
✅ Исправлено - настроена автоматическая оптимизация через image-minimizer-webpack-plugin:
- SVGO для SVG
- MozJPEG для JPEG
- OptiPNG для PNG
- Генерация WebP

### 6. Нет смены стилей под гео
✅ Исправлено - каждая геолокация имеет уникальную тему в `src/theme/geoThemes.ts`:
- EN - синие тона
- TR - красные тона
- IT - зеленые тона
- RU - темно-синие тона

### 7. README пустой
✅ Исправлено - создан подробный README с:
- Описанием архитектуры
- Инструкциями по установке и использованию
- Объяснением технических решений
- Ответами на замечания ревью

## 📝 Лицензия

MIT

## 👥 Контакты

По вопросам и предложениям: support@casinoroyale.com
